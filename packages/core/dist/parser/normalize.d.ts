/**
 * Formats the error message into human-friendly text
 *
 * @param input The raw error message from compilation
 * @returns The normalized error message
 */
export declare function normalizeError(input: string): string;
